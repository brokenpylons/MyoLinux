#include "buffer.h"
/*
Buffer::Buffer(const std::size_t size)
    : bytes(size)
{ }

Buffer::Container::pointer Buffer::data()
{
    return bytes.data();
}

std::size_t Buffer::size() const
{
    return bytes.size();
}
*/
